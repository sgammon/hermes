### === Hermes Upstream Config === ###
upstream hermes {
	server unix:/ns/runtime/sock/wsgi/hermes.sock;
}

upstream tracker {
	server unix:/ns/runtime/sock/wsgi/tracker.sock;
}

upstream realtime {
	server unix:/ns/runtime/sock/wsgi/realtime.sock;
}
